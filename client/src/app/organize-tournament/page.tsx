'use client';
import React from 'react';
import OrganizeForm from './organize';
import { DefaultService, TournamentDisplay} from '@/client';

const Page: React.FC = () => {
    return (
        <main>
            <OrganizeForm/>
        </main>
    );
};

export default Page;